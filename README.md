# Это предварительная версия для bank_widget
## В программе на данный момент реализованы следующие функции:
### 1. get_mask_card_number: Возвращает маску номера карты по правилу 'XXXX XX** **** XXXX'
#### Функция успешно тестирована через pytest, коэффициент покрытия кода 100%
### 2. get_mask_account: Возвращает маску номера счета по правилу '**XXXX'
#### Функция успешно тестирована через pytest, коэффициент покрытия кода 100%
### 3. mask_account_card: Возвращает строку по маске для счета или карты
#### Функция успешно тестирована через pytest, коэффициент покрытия кода 100%
### 4. get_date: Возвращает дату в формате ДД.ММ.ГГГГ
#### Функция успешно тестирована через pytest, коэффициент покрытия кода 100%
### 5. filter_by_state: Возвращает список словарей, отфильтрованный по полю 'state'
#### Функция успешно тестирована через pytest, коэффициент покрытия кода 100%
#### 1. Предварительно установленное значение для поля state -> EXECUTED
#### 2. Если это значение необходимо изменить - прямо укажите нужное значение при вызове функции
### 6. sort_by_date: Возвращает список словарей, упорядоченный по дате
#### Функция успешно тестирована через pytest, коэффициент покрытия кода 100%
#### 1. Предварительно установленное значение сортировки -> *по убыванию*
#### 2. Если это значение необходимо изменить - при вызове функции добавьте после вызываемого списка словарей запятую, и, через пробел, *'False'*
### 7. filter_by_currency: Bозвращает итератор, который поочередно выдает транзакции,где валюта операции соответствует заданной
#### 1. Предварительно установленное значение валюты -> *USD*
#### 2. Если это значение необходимо изменить - при вызове функции добавьте после вызываемого списка словарей запятую, и, через пробел, *'EUR'*
#### Функция успешно тестирована через pytest, коэффициент покрытия кода 100%
### 8. transaction_descriptions: Возвращает описание каждой операции по очереди по заданному ключу
#### Функция успешно тестирована через pytest, коэффициент покрытия кода 100%
### 9. card_number_generator: Выдает номера банковских карт в формате XXXX XXXX XXXX XXXX
#### Функция успешно тестирована через pytest, коэффициент покрытия кода 100%
### 10. Декоратор log: Записывает в лог начало и конец выполнения функции, а также ее результаты или возникшие ошибки
#### Для работы декоратора log определен внутренний декоратор wrapper, определяющий параметры функции, применяемой во внешнем декораторе
#### Декоратор успешно тестирован через pytest, коэффициент покрытия кода 100%
### 11. get_financial_transactions: Получает данные из внешнего JSONz-файла и преобразовывает в объект Python
#### Функция успешно тестирована через pytest, коэффициент покрытия кода 100%
### 12. currency_conversion: Принимает транзакцию и конвертирует из иностранной валюты в РУБЛИ с запросом на API сайт
#### Функция успешно тестирована через pytest, коэффициент покрытия кода 100%
### 13. process_all_transactions: Обрабатывает все транзакции и возвращает список конвертированных сумм в рублях
#### Функция успешно тестирована через pytest, коэффициент покрытия кода 100%
### 14. read_csv: принимающая путь к файлу, считывает информацию c CSV файла
#### Функция успешно тестирована через py### 14. read_csvО: принимающая путь к файлу, считывает информацию c CSV файла
### 15. read_excel: принимающая путь к файлу, считывает информацию c Excel файла
#### Функция успешно тестирована через py### 14. read_csvО: принимающая путь к файлу, считывает информацию c CSV файла
